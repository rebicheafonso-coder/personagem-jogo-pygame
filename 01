import_pygame

#Inicializando o Pygame
pygame.init()

#Definindo o tamanho da janela
Largura,altura = 720 720
Tela =pygame.display.set_mmode(largura,altura)
pygame.display.set_caption(nomedajanela)

loop principal do jogo
rodando=true

while rodando:
por evento in pygame.event.get()
if evento .type==pygame.QUIT:
rodando=false
#Atualizar a tela
pygame.display.flip()

Finalizar o pygame
pygame.quit()
